require './config/boot'

# # integrate rspec
# require 'rspec/core/rake_task'
# RSpec::Core::RakeTask.new('spec')
# task :default => :spec

desc 'Run the console'
task :console do
  require 'pry'
  ARGV.clear
  pry
end
